
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
    <style>
        /* Style de base */
        body {
            font-family: Arial, sans-serif;
            background-color: #1c1c1c;
            color: #f5f5f5;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            margin: 0;
        }

        .admin-list-container {
            display: flex;
            flex-direction: column;
            gap: 20px;
            width: 300px;
        }

        /* Style de la carte admin */
        .admin-card {
            display: flex;
            align-items: center;
            background-color: #2c2c2c;
            border-radius: 8px;
            padding: 15px;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.3);
            transition: transform 0.3s ease;
        }

        .admin-card:hover {
            transform: translateY(-5px);
        }

        .admin-avatar {
            font-size: 40px;
            background-color: #3c3c3c;
            width: 60px;
            height: 60px;
            display: flex;
            justify-content: center;
            align-items: center;
            border-radius: 50%;
            margin-right: 15px;
        }

        .admin-avatar i {
            color: #fff;
        }

        .admin-info {
            display: flex;
            flex-direction: column;
        }

        .admin-name {
            font-size: 18px;
            font-weight: bold;
            margin: 0;
            text-decoration: none;
            color: white;
        }

        .admin-status {
            font-size: 14px;
            color: #b0b0b0;
            margin: 5px 0 0;
            cursor: pointer;
        }

        .admin-status:hover {
            color: #fff;
            text-decoration: underline;
        }
    </style>

    <div class="admin-list-container">
        <?php if (!empty($admins)) : ?>
            <?php foreach ($admins as $admin) : ?>
                <div class="admin-card">
                    <div class="admin-avatar">
                        <img src="<?=$admin['picture']?>" style="heigh: 57px;width:57px; border-radius:50%">
                    </div>
                    <div class="admin-info">
                        <a href="AuthentificationAdmin.php?idAdmin=<?= $admin['idAdmin']?>" class="admin-name"><?= $admin['username'] ?></a>
                    </div>
                </div>
            <?php endforeach ?>
        <?php else : ?>
            <p>Aucun administrateur trouvé.</p>
        <?php endif ?>
    </div>
